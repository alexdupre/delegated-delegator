{
  "name": "delegated-delegator",
  "version": "2.0.0",
  "description": "Smart contracts to delegate FTSO delegation and reward claiming",
  "devDependencies": {
    "@nomicfoundation/hardhat-ignition-ethers": "^0.15.5",
    "dotenv": "^16.4.5",
    "hardhat": "^2.22.10"
  },
  "scripts": {
    "build": "hardhat compile",
    "deploy": "hardhat ignition deploy ignition/modules/DelegatedDelegatorFactoryModule.js",
    "verify": "hardhat verify"
  }
}
